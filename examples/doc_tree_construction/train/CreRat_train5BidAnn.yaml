# task
name: transducer_${data_type}_${random_seed}

# filepaths
data_type: CreRat_train5BidAnn

# training
device: cuda:0
skip_train: false

# hparams
random_seed: 127
train_docs: -1  # -1 means use all the training set
with_constraint: true
