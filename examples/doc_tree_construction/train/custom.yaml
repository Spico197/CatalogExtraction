# task
name: transducer_plm4w_lrscheduling_${train_docs}_${data_type}_${random_seed}

# filepaths
data_type: null
warmup_proportion: 0.1
plm_dir: data/wiki_plm_4w

# training
device: cuda:3
skip_train: false

# hparams
random_seed: 127
train_docs: -1  # -1 means use all the training set
with_constraint: true
